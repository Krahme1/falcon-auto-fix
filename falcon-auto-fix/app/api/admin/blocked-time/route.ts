import { NextRequest,NextResponse } from "next/server";import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/db";
export async function POST(req:NextRequest){if(!await getCurrentUser())return NextResponse.json({error:"Unauthorized"},{status:401});const b=await req.json();const start=new Date(b.start),end=new Date(b.end);if(!start.getTime()||!end.getTime()||end<=start)return NextResponse.json({error:"Invalid dates"},{status:400});const row=await prisma.blockedTime.create({data:{startTime:start,endTime:end,reason:b.reason||null}});return NextResponse.json({id:row.id})}
export async function DELETE(req:NextRequest){if(!await getCurrentUser())return NextResponse.json({error:"Unauthorized"},{status:401});const id=req.nextUrl.searchParams.get("id");if(!id)return NextResponse.json({error:"Missing id"},{status:400});await prisma.blockedTime.delete({where:{id}});return NextResponse.json({ok:true})}
