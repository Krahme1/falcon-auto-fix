import { NextRequest,NextResponse } from "next/server";import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/db";
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(!await getCurrentUser())return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const b=await req.json();const duration=Math.max(15,Math.min(480,Number(b.durationMinutes)||60));await prisma.service.update({where:{id},data:{durationMinutes:duration,active:Boolean(b.active)}});return NextResponse.json({ok:true})}
