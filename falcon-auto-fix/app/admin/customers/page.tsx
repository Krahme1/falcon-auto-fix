import { redirect } from "next/navigation"; import { getCurrentUser } from "@/lib/auth"; import { prisma } from "@/lib/db"; import { AdminNav } from "@/components/AdminNav";
export default async function Customers(){if(!await getCurrentUser())redirect("/admin");const customers=await prisma.customer.findMany({orderBy:{updatedAt:"desc"},include:{vehicles:true,appointments:{where:{status:"COMPLETED"},include:{service:true},orderBy:{startTime:"desc"},take:3}}});return <section className="adminShell"><AdminNav/><div className="adminMain"><p className="eyebrow">CRM</p><h1>Customers & service history</h1><div className="customerGrid">{customers.map(c=><article className="customerCard" key={c.id}><h3>{c.name}</h3><p>{c.phone}<br/>{c.email||"No email"}</p><h4>Vehicles</h4>{c.vehicles.map(v=><p key={v.id}>{v.year} {v.make} {v.model}</p>)}<h4>Recent completed work</h4>{c.appointments.length?c.appointments.map(a=><p key={a.id}>{a.service?.name||"Service"} — {a.startTime.toLocaleDateString("en-CA")}</p>):<p className="muted">No completed jobs yet.</p>}</article>)}</div></div></section>}
