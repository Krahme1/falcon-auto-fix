import { redirect } from "next/navigation";import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/db";import { AdminNav } from "@/components/AdminNav";import SettingsClient from "./SettingsClient";
export default async function Settings(){if(!await getCurrentUser())redirect("/admin");const [hours,blocks]=await Promise.all([prisma.businessHour.findMany({orderBy:{dayOfWeek:"asc"}}),prisma.blockedTime.findMany({where:{endTime:{gte:new Date()}},orderBy:{startTime:"asc"}})]);return <section className="adminShell"><AdminNav/><div className="adminMain"><p className="eyebrow">SHOP SETTINGS</p><h1>Hours & blocked time</h1><SettingsClient hours={hours} blocks={blocks.map(b=>({...b,startTime:b.startTime.toISOString(),endTime:b.endTime.toISOString()}))}/></div></section>}
