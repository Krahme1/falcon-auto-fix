import { getCurrentUser } from "@/lib/auth"; import AdminLogin from "./AdminLogin"; import { prisma } from "@/lib/db"; import { formatLocal } from "@/lib/time"; import { AdminNav } from "@/components/AdminNav";
export default async function Admin(){const user=await getCurrentUser(); if(!user)return <AdminLogin/>; const now=new Date(); const [pending,today,confirmed,recent]=await Promise.all([prisma.appointment.count({where:{status:"PENDING"}}),prisma.appointment.count({where:{startTime:{gte:new Date(now.getFullYear(),now.getMonth(),now.getDate()),lt:new Date(now.getFullYear(),now.getMonth(),now.getDate()+1)},status:{not:"CANCELLED"}}}),prisma.appointment.count({where:{status:"CONFIRMED",startTime:{gte:now}}}),prisma.appointment.findMany({where:{startTime:{gte:new Date(now.getTime()-86400000)}},orderBy:{startTime:"asc"},take:8,include:{customer:true,vehicle:true,service:true}})]);return <section className="adminShell"><AdminNav/><div className="adminMain"><div className="adminTop"><div><p className="eyebrow">OWNER DASHBOARD</p><h1>Good day, {user.name}.</h1></div><form action="/api/admin/logout" method="post"><button className="textButton">Sign out</button></form></div><div className="stats"><div><span>Today</span><strong>{today}</strong></div><div><span>Pending</span><strong>{pending}</strong></div><div><span>Upcoming confirmed</span><strong>{confirmed}</strong></div></div><h2>Upcoming appointments</h2><div className="tableWrap"><table><thead><tr><th>Time</th><th>Customer</th><th>Vehicle</th><th>Service</th><th>Status</th></tr></thead><tbody>{recent.map(a=><tr key={a.id}><td>{formatLocal(a.startTime)}</td><td>{a.customer.name}<small>{a.customer.phone}</small></td><td>{a.vehicle.year} {a.vehicle.make} {a.vehicle.model}</td><td>{a.service?.name||"Other"}</td><td><span className={`status ${a.status.toLowerCase()}`}>{a.status}</span></td></tr>)}</tbody></table></div></div></section>}
