"use client"; import { useState } from "react";
export default function ReminderButton(){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");async function send(){setBusy(true);const r=await fetch("/api/admin/reminders",{method:"POST"});const d=await r.json();setBusy(false);setMessage(r.ok?`${d.sent} reminder${d.sent===1?"":"s"} sent.`:(d.error||"Could not send reminders."))}return <div className="reminderAction"><button className="button ghost" disabled={busy} onClick={send}>{busy?"Sending…":"Send due 24h reminders"}</button>{message&&<small>{message}</small>}</div>}
