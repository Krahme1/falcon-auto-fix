import { redirect } from "next/navigation";import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/db";import { AdminNav } from "@/components/AdminNav";import ServiceEditor from "./ServiceEditor";
export default async function ServicesAdmin(){if(!await getCurrentUser())redirect("/admin");const s=await prisma.service.findMany({orderBy:{sortOrder:"asc"}});return <section className="adminShell"><AdminNav/><div className="adminMain"><p className="eyebrow">SETTINGS</p><h1>Services</h1><p className="muted">Change booking durations or hide a service from online booking.</p><div className="serviceAdminGrid">{s.map(x=><ServiceEditor key={x.id} service={x}/>)}</div></div></section>}
